# Frontend production environment
NODE_ENV=production
PORT=3000

# Public URLs exposed to the browser
# These values are embedded during `next build`
NEXT_PUBLIC_API_URL=https://chat.example.com
NEXT_PUBLIC_WS_URL=https://chat.example.com
